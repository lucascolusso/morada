- if user_signed_in?
  %p#intro
    Que tal
    = current_user.name
    ?
    %br/
    %span
      Compartilhe e veja fotos dos demais aquerenciados

- else
  %p#intro
    Quer ver fotos da querência?
    %br/
    %span
      Adentre a morada e compartilhe também as suas imagens

#posts

  - @posts.each do |post|
    .post
      .post_image
        = link_to (image_tag post.image.url(:small))
      .post_content
        .data.clearfix
          %p.buttons
            %span
              %i.fa.fa-comments-o
              = post.comments.count
            %span
              %i.fa.fa-thumbs-o-up
              = post.get_likes.size
